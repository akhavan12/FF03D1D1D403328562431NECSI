# Normalize

HTML → schema transformers, GPT-assisted passes, and validators.

Planned modules:
- `rules.py` — deterministic parsing and heuristics
- `gpt_passes.py` — html_to_markdown, extract_citations, summarize_page, infer_tags
- `validate.py` — JSON Schema validation and thresholds
